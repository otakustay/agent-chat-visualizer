---
description: 介绍src/modules下的编码要求
globs: src/modules/**
alwaysApply: false
---
## 项目模块

“模块”是系统中的一个概念，指代一个独立的业务功能，所有模块存放在`src/modules`目录下。

从技术角度，原则上一个模块对应一个顶级的路由，比如“产品介绍”对应路由`/about`，对应模块`/src/modules/About`目录。

每一个模块目录下有一个`index.tsx`，使用默认导出提供顶层路由对应的组件，如：

```tsx
export default function AboutModule() {
    return ...;
}
```

这些模块随后被`src/components/App.tsx`引入，如：

```tsx
import About from '@modules/About';

<Routes>
    <Route path="/about" elements={<About />} />
</Routes>
```

通常来说，模块下的`index.tsx`中的组件名称为模块名称加上`Module`后缀。
