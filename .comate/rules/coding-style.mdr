---
description:
globs:
alwaysApply: true
---
## 编码规范

### 关于注释

尽可能不要写注释，除非有一段相当复杂的代码需要说明。

### 模块引用

本项目设置了一个别名，`@`指代`src`目录。

在项目结构上，`src`目录下的第一层为大分类，如`utils`、`components`、`modules`，第二层为具体的功能模块。

在引用模块时，如果被引用的模块与当前模块处于`src`下不同的二层目录，则必须使用`@`来引用，不得使用相对路径。相对的，如果它们处于相同的二层目录，则必须使用相对路径。

示例：

- 从`src/modules/Foo/index.ts`引用`src/components/Bar`，必须使用`@/components/Bar`。
- 从`src/components/Foo/utils/misc/worker.ts`引用`src/components/Foo/config/default.ts`，必须使用`../../config/default`。

### 类型定义

类型定义应当尽可能地靠近使用定义的位置，当一个类型仅在一处使用时，类型写在同一个文件中，且不要导出。

当多个文件共享一个类型，且它们有明显的依赖关系时，在**被依赖**的文件中声明并导出类型，依赖的文件引入该类型。

当多个**没有依赖关系**的文件要共享一个类型时，类型需要被声明在在它们的共同祖先目录下的`interface.ts`文件中并导出，所有文件从该文件导入类型。

所有React的组件，其类型必须为组件名加`Props`后续，如组件`List`对应的是`ListProps`。

### 函数定义

处于模块顶层的函数，必须用函数定义的形式编写：

```ts
function foo() {
    // ...
}
```

嵌套在内部的函数，必须用箭头函数形式编写：

```ts
function Foo() {
    const handleClick = () => {
        // ...
    };
};
```

特殊情况如迭代器函数，可以遵循箭头函数的要求。
