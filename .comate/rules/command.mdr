---
description:
globs:
alwaysApply: true
---
## 项目命令规范

### 可用命令列表

项目提供以下核心命令：

- `npm run build`: **必须执行的情况**：
  - 任务结束后，必须运行确保代码可编译
  - 修改了多个文件或进行了重大代码变更时，必须在过程中验证编译正确性
  - 对于编译过程中出现的问题要及时修复，不得忽略编译错误

- `npm run lint`: **执行时机与处理原则**：
  - 任务结束后，必须运行 `npm run lint -- --fix` 检查代码质量
  - 对于简单的格式化问题（如缩进、分号等），可以直接修复
  - 对于逻辑性错误、类型错误、未使用变量等问题，应先尝试修复，如果修复可能影响业务逻辑或不确定如何修复时，则告知用户并让用户手动处理

### 执行优先级

代码质量验证的执行顺序：

1. 先运行 `npm run lint -- --fix` 修复代码质量问题
2. 再运行 `npm run build` 确保编译通过
3. 如果任何一步失败，必须修复后才能认为任务完成
